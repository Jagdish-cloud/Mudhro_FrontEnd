# ============================================
# Mudhro FinTech Backend - Environment Configuration Template
# ============================================
# Copy this file to .env and update with your actual values
# Command: cp env.template .env
# DO NOT commit .env to version control

# ============================================
# Server Configuration
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# CORS Configuration (IMPORTANT FOR PUBLIC-FACING APPS)
# ============================================
# For public-facing applications, you have two options:

# Option 1: Allow all origins (use with caution)
# Set to '*' to allow requests from any origin
# ALLOWED_ORIGINS=*

# Option 2: Whitelist specific domains (RECOMMENDED)
# Specify comma-separated list of allowed origins
# Include both http and https, and both www and non-www if needed
# Examples:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com
# ALLOWED_ORIGINS=https://mudhro.com,https://www.mudhro.com,https://app.mudhro.com,http://localhost:5173

# For development, defaults to: http://localhost:5173,http://localhost:3000
# For production, you MUST set this value
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Frontend URL Configuration
# ============================================
# Frontend URL for generating public links (e.g., agreement signature links)
# In development, this is typically: http://localhost:5173
# In production, this should be your frontend domain
# Examples:
# FRONTEND_URL=http://localhost:5173
# FRONTEND_URL=https://app.yourdomain.com
FRONTEND_URL=http://localhost:5173

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: In production, use a cryptographically secure random string
# Generate a secure secret: openssl rand -base64 32
# Minimum 32 characters required in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=2h
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================
# Database Configuration
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Mudhro_FinTech
DB_USER=postgres
# IMPORTANT: Use a strong password in production
# Never use default passwords in production
DB_PASSWORD=root

# ============================================
# Rate Limiting
# ============================================
# Set to false to disable rate limiting (NOT RECOMMENDED)
# Rate limiting helps prevent brute force attacks and DoS
RATE_LIMIT_ENABLED=true

# ============================================
# Email Configuration (Optional)
# ============================================
# Uncomment and configure if using email features
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# SMTP_FROM=noreply@yourdomain.com

# ============================================
# Security Notes
# ============================================
# 1. Never commit .env file to version control
# 2. Use strong, unique passwords for production
# 3. Generate secure JWT_SECRET using: openssl rand -base64 32
# 4. For public-facing apps, consider whitelisting specific domains instead of using '*'
# 5. Always use HTTPS in production
# 6. Regularly rotate secrets and passwords

